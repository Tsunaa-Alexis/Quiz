{# templates/admin/panel_categorie/panel_add_cat.html.twig #}

{% extends 'admin/panel_categorie.html.twig' %}
{% block main %}
    {{ form_start(form) }}
        <div class="container">
            <div class="form-group">
                {{ form_row(form.catName, {attr: {'class': 'form-control'}}) }}
            </div>
            <div class="form-group">
                {{ form_row(form.catDescription, {attr: {'class': 'form-control'}}) }}
            </div>
            <button type="submit" class="btn btn-primary mx-auto">
                <i class="fa fa-save" aria-hidden="true"></i> {{ 'Sauvegarder'|trans }}
            </button>
        </div>
    {{ form_end(form) }}
{% endblock %}